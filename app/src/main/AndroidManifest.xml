<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.TimeTableForICU.yasuhirachiba.timetableforicu">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_OWNER_DATA" />
    <uses-permission android:name="android.permission.READ_OWNER_DATA" />
    `
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".class_info.class_info"
            android:label="@string/title_activity_class_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".add_data.add_data" />
        <activity android:name=".class_info.gallery" />
        <activity android:name=".SettingsForTable" />
        <activity android:name=".modify_data.modify_data" />
        <activity
            android:name=".user_policy.User_policy"
            android:label="@string/title_activity_user_policy"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>